<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.trip.model.dao.AttractionDao">
	
	<select id="getSido" resultType="sidoDto">
		select sido_code, sido_name from sido
	</select>
	
	<select id="getGugun" resultType="gugunDto">
		select gugun_code, gugun_name from gugun where sido_code = #{sidoCode}
	</select>
	

 	<select id="getAttractionList" resultType="attractionDto">
		select first_image, title, addr1, latitude, longitude 
		from attraction_info 
		where content_type_id = #{content_id} and sido_code = #{sido_code} and gugun_code = #{gugun_code} 
		limit 10
	</select> 

 	<select id="searchByKeyword">
		SELECT 
		content_id, content_type_id, title, addr1, first_image, latitude, longitude 
		FROM attraction_info 
		WHERE title LIKE CONCAT('%', #{keyword}, '%') 
		limit 10
	</select>
	
	<select id="getSidoInfo" parameterType="String">
		select * from sido_info where sido_code = #{sido_code}
	</select>
	 

</mapper>
